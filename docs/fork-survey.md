# Что люди сделали с Elevator Saga за 12 лет

Обзор собран **10 августа 2026 года**. В оригинальном репозитории
([magwo/elevatorsaga](https://github.com/magwo/elevatorsaga)) ветка `master` не двигалась с
4 января 2021 года (`e0c55bf8`, правка README), а последний коммит, менявший код, — `9a2441af` от
21 июля 2015 года. Дата 21 ноября 2022 года, на которую легко попасться, — это последний _push_, то
есть ветка из PR #137, а не коммит в `master`. Форков при этом **351** (на 12 августа
2026 года) — почти всё, что произошло с игрой за эти годы, произошло именно там, и почти нигде на
это нет ссылок. Открытый трекер оригинала разобран отдельно, в
[`docs/upstream-triage.md`](upstream-triage.md).

Это список покупок, а не архив. Всё, что ниже, попало сюда потому, что это можно взять себе: фича,
починенный баг или урок от того, кто уже попробовал и упёрся в стену.

Сверено с репозиторием 12 и 25 августа 2026 года: что сделано — помечено «**Сделано:**» и названо
именем символа, по которому это видно. Ссылки на наш код именные, а не построчные — номер строки
протухает на следующем коммите, а `grep` по имени переживает любой сдвиг. Исключение — ссылки на
`legacy-1.x`: это тег, его содержимое неизменно, и там номер строки честнее имени.

## Как это собиралось и чего здесь нет

Все 352 форка получены через GitHub API и отфильтрованы: форк, у которого последний push отстоит на
день-два от момента создания, без звёзд и с тем же числом файлов, что у оригинала, — это чьё-то
сохранённое решение уровня. Осталось **57 кандидатов**, из них **29 сравнены с оригиналом** (запрос
`compare/master...owner:branch` возвращает точное число коммитов впереди и все сообщения коммитов за
один вызов). Из этих 29 **впереди оказались 18**, остальные — форки без собственных коммитов, просто
отставшие. Верхушку списка потом читали напрямую: README, диффы, `package.json` — через
`raw.githubusercontent.com`.

Всё в разделе про форки проверено именно так. **Оставшиеся 28 кандидатов не сравнивались**
(неавторизованный API — 60 запросов в час, обзор съел 33), как и ~295 форков, отброшенных фильтром:
фильтр хороший, но не идеальный, там ещё может что-то лежать.

**Главная дыра метода — не в фильтре.** Обход по сети форков в принципе не видит тех, кто просто
скопировал игру в новый репозиторий, не нажав Fork: у GitHub такой репозиторий не связан с
оригиналом ничем. Найденные так копии (`zgca-forge/Elevator`, `avanderw/elevator-saga`) попали сюда
из веб-поиска, и одну из них первая версия этого документа успела записать в «дословное зеркало» по
одной только опубликованной странице — исправлено 11 августа 2026 года, см. раздел ниже. Вывод на
будущее: страница ничего не доказывает, смотреть надо репозиторий.

Разделы после списка форков собраны отдельным поиском по англо-, русско-, китае- и японоязычным
источникам. У каждого утверждения там помечено, что именно было прочитано.

Всё изученное — MIT, унаследованный от оригинала, с неизменённым `LICENSE.txt`: код можно брать с
указанием авторства, а не только переписывать своими руками. Единственное исключение —
[2xh](https://github.com/2xh/elevatorsaga), который переименовал файл в `LICENSE`, и его содержимое
не проверялось. Всё, что мы возьмём из форка, должно получить строчку в нашем `LICENSE.txt` с именем
автора.

## Форки, которые реально впереди оригинала

По убыванию того, насколько нам это нужно.

### Стоит взять

#### [avodonosov/elevatorsaga](https://github.com/avodonosov/elevatorsaga) — 5 коммитов впереди

[играть](https://avodonosov.github.io/elevatorsaga/) ·
[дифф](https://github.com/magwo/elevatorsaga/compare/master...avodonosov:elevatorsaga:master)

**Саммари:** сид прогона печатается в консоль и принимается из URL, так что упавший прогон можно
повторить на том же самом здании; плюс дольше всех ждущий пассажир подсвечивается красным. Лучшая
идея во всей сети форков и одна из самых дешёвых.

Две вещи:

1. **Воспроизведение по сиду.** Каждый прогон генерирует сид и печатает его в консоль вместе с двумя
   способами повторить прогон: `?seed=…` в URL или `window.GameSeed = "…"`. Когда решение заваливает
   уровень по времени ожидания, можно запустить _то же самое здание_ ещё раз, а не гадать.
2. **Дольше всех ждущий пассажир рисуется красным**, а уходящие пассажиры сильнее приглушены. Это
   ответ на вопрос «почему у меня взлетело максимальное ожидание», нарисованный прямо на картинке.

README этого форка честен насчёт того, что не получилось: сида для `Math.random` **не хватило для
детерминированного воспроизведения** — «в редких случаях повтор ведёт себя иначе, чем оригинальный
прогон», и автор грешит на тайминги анимации. Он был прав, и диагноз был общий, а не его личный:
внутренний цикл подшагов ограничивал шаг сверху `dtMax`, но сумма за кадр — это `t - lastT` из
`requestAnimationFrame`, то есть остаточный подшаг каждый раз свой. Программу игрока спрашивали в
чуть-чуть разные моменты, и два интерактивных прогона одного сида расходились.

**У нас этого ограничения нет.** Симуляция идёт фиксированным тиком: и `codeObj.update`, и физика
мира двигаются ровно на `TICK_SECONDS` (`src/game/world-controller.ts`), а неотработанный остаток
переносится аккумулятором на следующий кадр, а не округляется в нём. Прогон стал функцией числа
прошедших тиков, а не того, как таймер браузера поделил на них время, и это измерено, а не заявлено:
`src/game/determinism.test.ts` гоняет один сид через настоящий `WorldController` на расходящихся
расписаниях кадров — 60 Гц против 120 Гц, 60 Гц против 60 Гц плюс наносекунда, 60 Гц против дрожащих
часов — и сверяет положения кабин, тайминг прибытий и счётчики нажатий. Так что **сид повторяет и
здание, и поток пассажиров, и сам прогон**, при условии, что игрок не поменял программу. Не
повторяется только то, сколько тиков прогон успеет, если его не останавливать, — но проигранный
прогон кончается по условию уровня, а не по секундомеру.

**Сделано:** сид принимается как `seed=…` в хеше, печатается в консоль и живёт блоком в поповере
настроек (`src/features/manage-seed/ui/seed-panel.ts`). Подсветка дольше всех ждущего пассажира —
жёлтая, а не красная: пассажир стоит на полосе `--color-world` и ездит внутри кабины
`--color-elevator`, и красный `#d54` даёт на них 1,54:1 и 1,07:1 при нужных по WCAG 1.4.11 трёх
(правило `.person.is-waiting-long` в `src/entities/passenger/ui/passenger-view.css`).

#### [2xh/elevatorsaga](https://github.com/2xh/elevatorsaga) — 19 коммитов впереди, своя версия 1.8.0

[играть](https://2xh.github.io/elevatorsaga/) ·
[дифф](https://github.com/magwo/elevatorsaga/compare/master...2xh:elevatorsaga:master)

**Саммари:** самый функциональный браузерный форк: destination dispatch, шесть дополнительных
методов API, кнопки скорости, пользовательские уровни через JSON и фитнес в воркере. Брать стоит не
всё — часть меняет физику лифтов и ломает сравнимость результатов.

По истории коммитов: режим destination dispatch, переписанные под него уровни, **разная высота
этажей**, фитнес-прогон в **воркере**, «меньше обновлений на паузе» и изменение скорости лифтов с
обновлением спеков. Страница документации добавляет `getExactCurrentFloor()`,
`getExactFutureFloorIfStopped()` («точный этаж, на котором лифт остановится, если начать тормозить
сейчас»), `isApproachingFloor()`, `isFull()`, `isEmpty()` и `getMaxSpeed()`, плюс кнопки ➕/➖ для
скорости времени, пользовательские уровни и правый клик по Save для переключения автосохранения.

**«Добавляет» тут значит «выводит наружу», и это важно для оценки цены.** Пять из шести методов —
`getExactCurrentFloor`, `getExactFutureFloorIfStopped`, `isApproachingFloor`, `isFull`, `isEmpty` —
существовали на `Elevator` ещё в 1.6.5: они лежат прямо в `elevator.js` под тегом `legacy-1.x`, и
движок ими сам же и пользуется. Шестой, `getMaxSpeed()`, — новый метод, но и он лишь
отдаёт давнее публичное поле `MAXSPEED`. То есть 2xh написал не физику, а фасад, и вопрос про эти
методы всегда был не «сколько стоит реализовать», а «что мы обещаем, выпуская их в публичный API» —
разбор ниже про это и есть.

**Как именно устроены пользовательские уровни** (прочитано в `app.js`, `challenges.js`,
`presenters.js`, `world.js`):

- Открываете несуществующий номер уровня — `app.startChallenge()` видит, что `challenges[i]` нет, и
  показывает обычный `prompt()`: _«Challenge #N not found, input options in JSON format to create
  one»_. Вы вводите JSON с опциями мира, он парсится и кладётся в массив уровней с условием
  `requireNothing()`.
- Опции берутся те же, что у конструктора мира: `floorCount`, `floorHeights` (массив, циклически по
  этажам — отсюда «разная высота этажей»), `elevatorCount`, `elevatorCapacities`, `elevatorSpeeds`
  (по умолчанию `[3]`), `startFloors` (по умолчанию `[0]`), `spawnRate` и `lobbyPossibility` (по
  умолчанию 0.5 — в оригинале доля «из лобби» зашита намертво).
- Чтобы получить заготовку для правки, есть правый клик по кнопке старт/стоп: он выводит
  `Challenge #N options: {…}` текущего уровня прямо на страницу, в блок сообщений фитнес-сьюта.
  Правый клик по Apply туда же выводит опции фитнес-прогона и его статистику.

Ограничение, которое надо понимать: условие всегда `requireNothing()`, то есть пользовательский
уровень — это **песочница без цели**, живущая только в памяти вкладки (после перезагрузки она
пропадает). Конструктора условий победы там нет. То есть «редактор уровней», о котором просят люди,
здесь реализован ровно наполовину, зато десятью строчками.

_Что брать:_ песочница с параметрами — самый дешёвый ответ на самый частый запрос (см. ниже), и она
не требует никакого UI. Destination dispatch — это уже дизайнерское решение, а не патч: это
единственная реальная лифтовая концепция, которой в оригинале нет, и её просят, но она меняет смысл
уровней.

**Сделано, и по обеим претензиям к 2xh лучше.** Песочница — `createSandboxLevel`, `requireSandbox` и
`SandboxOptions` (`src/game/levels.ts`), параметры разбирает `resolveSandboxOptions`
(`src/pages/game/model/route.ts`). Первое отличие: параметры лежат в адресной строке, а не в памяти
вкладки, так что здание переживает перезагрузку и им можно поделиться ссылкой. Второе: вместо сырого
JSON из `prompt()` каждое значение зажимается в границы `SANDBOX_LIMITS`, а зажатое печатается
обратно в полосу цели над зданием, чтобы игрок видел, во что превратилось написанное. Список «не
брать без разговора» соблюдён буквально: `elevatorSpeeds`, `floorHeights` и `lobbyPossibility` в
`WorldOptions` отсутствуют, так что физика и доля «из лобби» из URL недостижимы.

Оговорка: **сам `WorldOptions` с тех пор вырос** — добавились `elevatorServedFloors`,
`trafficProfile` и `destinationDispatch` (глава 2 ниже). Три отвергнутых поля отвергнуты
по-прежнему и по той же причине, но теперь это отдельное решение, а не следствие общего правила «в
опции мира ничего не добавляем».

_Что не брать без разговора:_ смена скорости лифтов и высоты этажей ломает сравнимость результатов с
оригиналом, которую мы пока держим. Из методов спорный ровно один —
`getExactFutureFloorIfStopped()`, ему посвящён отдельный разбор ниже; `isApproachingFloor()`,
`isFull()` и `isEmpty()` были безопасны и **уже добавлены** — они на `ElevatorInterface`
(`src/game/elevator-interface.ts`), в документации и в автодополнении.

#### [jaredkrinke/elevatorsaga](https://github.com/jaredkrinke/elevatorsaga) — 2 коммита впереди

Предложено оригиналу как до сих пор открытый
[PR #137](https://github.com/magwo/elevatorsaga/pull/137) · демо:
<https://jaredkrinke.github.io/elevatorsaga/>

**Саммари:** меняет CodeMirror на Monaco и добавляет TypeScript-декларации, то есть настоящий
автокомплит по API лифтов — самая востребованная фича во всём обзоре, уже написанная. Ценна здесь не
замена редактора, а декларации.

**Обе половины у нас сделаны.** Автодополнение по API лифта и этажа работает в CodeMirror
(`playerApiCompletionSource` в `src/ui/completions.ts`), Monaco не взят и не будет. Собственный
`.d.ts` — `public/elevatorsaga.d.ts`, уезжает в `dist/` как есть; `src/api-declarations.test.ts`
держит его на фасадах: имена читаются с живых `ElevatorInterface` и `FloorInterface` по цепочке
прототипов, а не выписываются вторым списком. Написан руками, а не сгенерирован — в `tsconfig.json`
нет `declaration`.

Автор — создатель SIC-1. Оригинал так и не смёржил: «Понимаю, что репозиторий не поддерживается, но
открываю pull request, чтобы о нём знали».

_Цена для нас:_ мы сознательно перешли на CodeMirror 6 и вынесли его в **два** чанка, а не в один:
`codeSplitting.groups` в `vite.config.ts` отделяет `editor-grammar` (`@lezer/*`) от `editor-vendor`
именно затем, чтобы ни один не подошёл к порогу предупреждения в 500 КБ. Перемеряно на сборке `main`
25 августа 2026 года: 132 129 и 347 894 байта, то есть 47 447 и 110 629 байт под `gzip -9`
(в первой редакции стояли замеры от 12 августа — 131 864 и 365 177 байт). Monaco в несколько раз
тяжелее и не tree-shake-ится. Ценная половина — **декларации типов**, и она взята: собственный
`.d.ts` кормит автокомплит в любом редакторе и снимает дублирование работы, которую три человека
проделали независимо друг от друга
([гист Josef37](https://gist.github.com/Josef37/e075b6a005a47d146c7e7ab9ed7ae893), заведённый как
[#133](https://github.com/magwo/elevatorsaga/issues/133);
[гист steinuil](https://gist.github.com/steinuil/21b49b96eaaac4b792a0c69a7d82a4f9);
`bekk/elevator-saga-ts`). Разница с их гистами в том, что наши фасады написаны на TypeScript: тест
сверяет декларацию с ними, а не с чьей-то памятью.

### Стоит прочитать, прежде чем делать то же самое

#### [minimusubi/elevatorsaga](https://github.com/minimusubi/elevatorsaga) — **64 коммита впереди**

Последний push — апрель 2025, ветка по умолчанию `main`.

**Саммари:** параллельная модернизация той же игры и ближайший конкурент этому репозиторию — те же
задачи, решённые независимо и местами иначе; в частности, там приняли ломающее изменение, от
которого мы отказались.

Полный перевод на TypeScript, Monaco, lodash заменён на radashi, ESLint 9 + Prettier,
CSS-переменные, убрана Google Analytics, `Floor` → `FloorInterface` в пользовательском коде,
приватные слушатели эмиттера, перехват ошибок в обработчиках событий, экранирование HTML в выводе
ошибок.

**Ломающее изменение, которое они приняли, а мы — нет:** коммит
`refactor!(Emitter): first argument of event handlers changed from string to EmitterEvent` — первым
аргументом обработчика события вместо строки с именем события теперь приходит объект `EmitterEvent`;
плюс, в том же коммите, «deprecate some events/methods». Любое чужое решение, написанное за 12 лет
для оригинального API, после такого нужно править. Мы этот аргумент оставили строкой ровно ради
того, чтобы старые решения из вики и с гитхаба продолжали запускаться без изменений. Их проект
собирается голым `tsc` плюс скрипт копирования, без сборщика.

_Вывод:_ целиком брать нечего, сравнивать — есть с чем. Если понадобится второе мнение по решению в
`src/game/observable.ts`, смотреть надо сюда.

#### [chrismooredev/elevatorsaga](https://github.com/chrismooredev/elevatorsaga) — 35 коммитов впереди

**Саммари:** ещё один перевод на TypeScript, зашедший дальше нас в одну сторону — код игрока как
ES-модуль — и починивший то, на что люди жалуются: нормальный стек ошибки пользовательского кода.

Примерно 2020 год. Переход на ES-модуль для пользовательского кода помечен в истории как явное
ломающее изменение; вторая интересная строчка — «make users' error call stack look better». Плюс
Monaco и более строгие типы для обсервабла и его наследников.

#### [cornacchia/elevatorsaga-blockly](https://github.com/cornacchia/elevatorsaga-blockly) — 12 коммитов, ~300 файлов

**Саммари:** игра, перестроенная вокруг Blockly — блоки вместо кода, учебник и «упражнение на
отладку»; доказательство, что игру используют в классах.

[Blockly](https://developers.google.com/blockly) — drag-and-drop блоки вместо написания JavaScript,
плюс написанное руководство, **упражнение на отладку** и пояснительные тексты. Явно сделано для
преподавания. Мёржить нечего, но идея «упражнения на отладку» — дать игроку сломанное решение и
попросить починить — отличный формат уровня, который структурно нам ничего не стоит.

**Сделано, и это ровно та форма.** Учебный трек — `TutorialLevel` в `src/game/tutorial.ts`: у
каждого из восьми уровней есть `startingCode` (сломанная программа) и `solutionCode` (починенная),
а `tutorial-solutions.test.ts` доказывает прогонами, что выданная программа уровень проваливает, а
показанный ответ выигрывает — то есть что упражнение действительно упражнение, а не просто текст;
`tutorial-sweep.test.ts` держит то же самое на четырёхстах сидах для трёх уровней с наименее
устойчивым исходом. Маршрут `#level=tutorial-N`, панель с подсказками и разбором рядом с редактором,
восемь плиток в меню уровней, и ни одна из них ничего не запирает: пройденные помечены в списке, а
не открывают следующие.

#### [DiscoElevator/elevatorsaga](https://github.com/DiscoElevator/elevatorsaga) — 39 коммитов впереди

**Саммари:** мультиплеерная ветка — сервер, аккаунты, хранение кода и рейтинг, плюс русская
документация; полезно как разведка того, что влечёт за собой таблица лидеров.

Игровой сервер, учётные записи с релогином по токену, отдельное хранилище кода для каждого
пользователя и **рейтинг**. Плюс переезд в `src/`, вебпаковые `npm run dev`/`build` и **русский**
`documentation_RU.html`. Самый тяжёлый форк в обзоре и, скорее всего, уже не запускающийся — читать
как предостережение.

#### [didil/gowasm-elevatorsaga](https://github.com/didil/gowasm-elevatorsaga) — 16 коммитов, 15 звёзд

Самый популярный форк · [играть](https://didil.github.io/gowasm-elevatorsaga/)

**Саммари:** решения пишутся на Go, компилируются в WASM серверной сборкой в Docker и подгружаются в
страницу; ценна не сборка, а доказательство, что движок можно водить из другого языка.

Результат сборки кэшируется по хешу. Сервис сборки копировать не стоит; интересна прослойка JS↔WASM,
которая связывает движок с чужим рантаймом.

#### [mostafa-hz/elevatorsaga](https://github.com/mostafa-hz/elevatorsaga) — 12 коммитов впереди

**Саммари:** агенты обучения с подкреплением, играющие прямо в браузере; вместе с ednussi — второй
независимый запрос на детерминированный headless-прогон. **Сделано.**

`agents.js`, импорт/экспорт модели, настройка функции награды, управление исследованием. Вместе с
[ednussi/ElevatorSaga](https://github.com/ednussi/ElevatorSaga) (пересимуляция на Python для
Q-learning, DQN и мультиагентных экспериментов) это второй независимый запрос на одно и то же:
**детерминированный headless-раннер**, на который можно натравить бота и посчитать результат по
многим сидам. **Сделано:** `npm run bench -- program.js` — `runBench` в `src/cli/bench.ts`: тот же
фитнес-сьют, те же три здания и тот же список сидов, что и по кнопке в игре, без браузера; `--seeds`
задаёт свои здания, `--json` отдаёт числа неокруглёнными. Проигрыша он не различает и не может —
фитнес-сьют гоняет здания без условия победы (`requireNothing` в `src/game/fitness.ts`), результат
здесь числа, а не вердикт. Отчёт занимает весь stdout, а всё, что печатает сам прогон, уходит в
stderr, иначе одна отладочная строка ломала бы разбор JSON.

#### [WebCabin/elevatorsaga](https://github.com/WebCabin/elevatorsaga) — 6 коммитов впереди

**Саммари:** редактор кода целиком заменён на визуальный редактор графа узлов.

[wcPlay](https://github.com/WebCabin/wcPlay) вместо CodeMirror. Тот же инстинкт, что у форка с
Blockly, только другого десятилетия.

### Локализации и что из них следует

**Саммари:** три независимые попытки перевода — японская
[shoheihagiwara](https://github.com/shoheihagiwara/elevatorsaga), японская
[Double-oxygeN](https://double-oxygen.net/elevator-saga/) и греческая
[gamesedu/elevatorsaga-gr](https://github.com/gamesedu/elevatorsaga-gr), — ни одна не знала о
других, и каждой пришлось форкать игру целиком, потому что точки расширения для локализации нет.
**У нас она есть:** строки лежат в типизированном каталоге (`src/i18n/`), новый язык — это один файл
каталога и одна строка в двух местах, без форка.

Полезное здесь одно, и оно не про перевод: у gamesedu **строки стартового кода недоступны для
правки**, доведённые за четыре коммита до состояния «заблокирована только первая строка». Это
преподавательский приём — выдать скелет, который ученик не сломает, — и он стоит пунктом в списке
«Что делать дальше».

### Упомянуто для полноты

- **[VyunSergey/elevatorsaga](https://github.com/VyunSergey/elevatorsaga)** — 5 коммитов: коллекция
  чужих опубликованных решений плюс `alg_stats.txt` с их сравнением. Готовый корпус для бенчмарка:
  цикл по файлам с `npm run bench` и одним и тем же `--seeds` даёт колонку чисел, полученных на
  одних и тех же зданиях.
- Ещё пять форков по три-пять коммитов, переносить нечего: порты на Python и Java (`raux`),
  «updated challenge» без прочитанных диффов (`codeskyca`), RxJS на первом уровне
  (`AbdellaToronto`), ребрендинг (`ejrv`), стили для встраивания в чужую страницу (`David-McEwen`).

## Производные вне сети форков

Из веб-поиска. «Прочитано» значит, что страница открывалась.

- **[zgca-forge/Elevator](https://zgca-forge.github.io/Elevator/index.html)**
  (`pip install elevator-py`) — **настоящий движок на Python с HTTP-API клиент/сервер и двумя
  вещами, которых нет в браузерной игре.**

  Это **учёт энергопотребления** с отдельным расходом у каждого лифта и физическое ускорение.
  Энергия или ходы как вторая метрика дают уровню вторую ось оптимизации без новых механик.
  _(Прочитано: индекс документации.)_

- **[Cargo Dispatch](https://matthuggins.com/lab/cargo-dispatch)** (Show HN, март 2026,
  [тред](https://news.ycombinator.com/item?id=47302772)) — **ближайший живой аналог: тот же жанр на
  TypeScript, только про складских роботов.**

  Пять уровней, переключатель скорости 0.5×/1×/2×/4×, раздельные режимы Edit/Run и живая панель
  статуса каждого робота — последние две вещи оттуда дёшево берутся. Нет повторов, сидов, шаринга и
  редактора уровней, и Elevator Saga там не упомянута. _(Прочитано: страница игры.)_

- **[avanderw/elevator-saga](https://github.com/avanderw/elevator-saga)**
  ([играть](https://avanderw.co.za/elevator-saga/)) — **не зеркало: копия 1.6.5 с быстрым
  переключением уровней, новой метрикой «средняя загрузка на ход» и вынутым редактором — страница
  крутит собственный алгоритм автора.**

  Отдельный репозиторий, а не форк в терминах GitHub (создан 16 октября 2025-го, MIT, без родителя),
  поэтому обход по сети форков его не видел, а по опубликованной странице он выглядит как оригинал:
  это клон с полной историей magwo, залитый в чистый репозиторий, а не копия файлов. Диффы против
  1.6.5 (сверено с `legacy-1.x`) точечные в движке и крупные в оболочке. Интересно вот что:

  - **Быстрая навигация по уровням** — ряд кнопок 1–18 прямо под полем уровня, тогда как у оригинала
    есть только ссылка «Next challenge». Самая полезная мелочь во всём репозитории. **Сделано, и без
    их ошибки:** меню складывает `buildLevelMenu`, рисует `presentLevelSwitcher`
    (`src/widgets/level-switcher/`). У avanderw обработчик присваивает хеш целиком, и прыжок стирает
    `timescale` и `autostart`; наши ссылки строит `createParamsUrl`
    (`src/shared/lib/route-query.ts`), который переносит все параметры и выбрасывает ровно один —
    `seed`, потому что сид выпал для покидаемого здания и на другом уровне не значит ничего.
  - **Новая метрика: `avgLoadFactorOnMove`** — лифт на каждом ходе шлёт свою загрузку, мир копит
    сумму и делит на `moveCount`. Четыре правки, вторая ось оптимизации почти даром — та же идея,
    что энергия у `zgca-forge`, только считается из того, что уже есть.
  - **Редактор кода отключён**: `getCodeObj()` возвращает алгоритм автора из `ai/v3.js`, а `reset` и
    `setCode` до сих пор дёргают `cm.setValue(...)` у несуществующего объекта. Как игра это шаг
    назад, как стенд для одного алгоритма — ровно то, что нужно автору.
  - **Шестнадцать markdown-разборов** рядом с кодом: архитектура v1/v2/v3, дорожная карта, отчёты по
    тестам. Большая часть — про их алгоритм, но две записки касаются движка. `FULL_ELEVATOR_FIX.md`
    — верный факт: загрузка почти никогда не равна 1.0, потому что пассажиры весят 55–100 кг,
    поэтому у них порог 0.7. `LOAD_FACTOR_TIMING.md` — верный симптом при неверном объяснении: в
    `stopped_at_floor` загрузка действительно ещё старая, но не потому, что «`loadFactor()` не
    real-time» (он считается по слотам в момент вызова), а потому, что это событие приходит до
    посадки — раньше `exit_available`/`entrance_available` (`Elevator.handleDestinationArrival` в
    `src/game/elevator.ts`, у них `elevator.js:144`), а кнопку в кабине пассажир жмёт уже после
    входа.

  _(Прочитано: репозиторий, диффы против `legacy-1.x`, README и два разбора.)_

## На что жалуются и что ломается у людей

С нашим положением по каждому пункту. Наша часть — про этот репозиторий, чужая — по ссылкам.

| Что сообщают                                                                                                                                                                                                          | У нас                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Пассажиры не заходят в лифт, который остановился по пути** — [#124](https://github.com/magwo/elevatorsaga/issues/124), [larschdk на HN](https://news.ycombinator.com/item?id=27487111)                              | Не починено — и чинить нечего. Игрок зовёт `stop()` из обработчика `passing_floor`; кабина в этот момент едет, и ближайшая точка, до которой она физически может доехать, — та, куда она докатится, а это уже за этажом. Посадку предлагают только по прибытии на этаж, а на этаж кабина так и не приезжает. `stop()` об этом прямо говорит в справке («лифт, скорее всего, встанет не на этаже, так что пассажиры не выйдут»), а нужное игроку пишется как `goToFloor`. Сценарий воспроизведён целиком в `src/game/world.test.ts`, блок «stopping en route», вместе с однострочной правкой, после которой тот же пассажир заходит. До 2026-08-12 в этой клетке стояло «Починено, с тестами» со ссылкой на пункт `#59/#74/#98/#124` в README — это была ошибка: те три номера про стоящий лифт с неверным индикатором, и никакой индикатор не поможет кабине, которая встала между этажами.                                                                                               |
| **Сам magwo: «то, что дополнительные люди не заходят, — архитектурная проблема игры»** — [HN, 2015](https://news.ycombinator.com/item?id=8929314)                                                                     | Это корень всего семейства. Наше повторное предложение места при смене индикатора плюс задержка на посадку закрывают случаи со _стоящей_ кабиной. Случай с кабиной на ходу (#124, строка выше) они не закрывают и не могут: там дело в том, где кабина встала, а не в том, что она показывает.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Два бесплатных хода на лифт в начале** — [avereveard на HN](https://news.ycombinator.com/item?id=8929314), [#117](https://github.com/magwo/elevatorsaga/issues/117)                                                 | Починено; `moveCount` начинается с 0, закреплено тестами.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Пассажиры не нажимают кнопку вызова повторно** — [#110](https://github.com/magwo/elevatorsaga/issues/110)                                                                                                           | Починено, с тестами.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Синтаксические ошибки проглатываются catch'ем логгера** — [juloo](https://news.ycombinator.com/item?id=27487111), [Lobsters](https://lobste.rs/s/w1dac5/), [mschaef](https://news.ycombinator.com/item?id=37306262) | Ошибки компиляции и рантайма показываем в баннере, на обе есть e2e-тесты. Достаточно ли хорош сам _текст_ — отдельный вопрос, стоит посмотреть.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **`world.transportedCounter = 999999` в `init` выигрывает любой уровень** — [benwaffle, 2015](https://news.ycombinator.com/item?id=8933287), [a2h, 2022](https://news.ycombinator.com/item?id=33273543)               | **До сих пор работает и у нас**: `App.#startRun` (`src/pages/game/index.ts`) кладёт мир на `window` (как `legacy-1.x:app.js:169`), а код игрока исполняется в глобальной области. Сделано намеренно — это тот самый отладочный крючок из консоли. Разбор вариантов починки — в следующем разделе.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **`checkDestinationQueue()` после правки очереди «ужасно избыточен»** — Ideka, [swyx: «документации нужно много работы»](https://news.ycombinator.com/item?id=33249988)                                               | Задокументировано, не изменено. Сделать вызов неявным — реальный вариант.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **«На мои обработчики отвечает только последний лифт»** — [#111](https://github.com/magwo/elevatorsaga/issues/111), дубль [#138](https://github.com/magwo/elevatorsaga/issues/138)                                    | Проверено: движок ни при чём. У каждого лифта свой источник событий, и обработчик получает свой лифт в `this` — на это есть тест «gives every elevator its own events rather than the last one's» в `src/game/world-controller.test.ts`. Ловушка в `var` в цикле, который эти обработчики вешает: `var` даёт одну привязку на всю функцию, поэтому к моменту вызова все обработчики видят тот лифт, на котором цикл закончился. Тот же тест, переписанный через `var`, воспроизводит жалобу дословно — отзывается только последний лифт. **Сделано:** в документации по API об этом сказано там, где игрок с ловушкой и столкнётся — ключ `docs.examples.events.perElevator.html`, среди примеров на странице справки.                                                                                                                                                                                                                                                                    |
| **Уровень 6 проходится, если морить голодом всех выше первого этажа** — [pavel_lishin](https://news.ycombinator.com/item?id=8929314)                                                                                  | Проверено, и всё оказалось интереснее. Дословный вариант — челнок между двумя нижними этажами — проигрывает: пассажиры с верхних этажей занимают места в кабине и не выходят никогда (6 перевезённых на 60 перемещениях). А вот «дать зданию накопиться и потом пройти его одним рейсом» работает: уровень 6 закрывается за 28 перемещений вместо 48 при максимальном ожидании 631 с, а уровень 7 честным немедленным рейсом **не проходится вовсе** (перемещения кончились на 86 перевезённых из нужных 100: `challenges[6]` — это `requireUserCountWithinMoves(100, 63)`) и проходится за 51, если 200 с не возить никого. Лимит именно 63; 60 — это лимит шестого уровня, `challenges[5]` с `requireUserCountWithinMoves(40, 60)`, и первая версия этого абзаца их перепутала. На уровнях с лимитом перемещений ожидание не просто ничего не стоит — оно выигрышная стратегия. Параметры уровней оригинальные, менять их значит менять игру, так что это вопрос продуктовый, а не баг. |
| **Скачок сложности заставляет переписывать всё, и не говорят, какой пассажир вышел за лимит** — [SamBam](https://news.ycombinator.com/item?id=27487111), [#135](https://github.com/magwo/elevatorsaga/issues/135)     | Половина закрыта: дольше всех ждущий пассажир подсвечивается — жёлтым, а не красным, по контрасту (разбор у форка avodonosov выше). Скачок сложности между уровнями остаётся открытым.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

Все ссылки в таблице прочитаны через Algolia API HN или на страницах самих issue, кроме
[#111](https://github.com/magwo/elevatorsaga/issues/111)/[#138](https://github.com/magwo/elevatorsaga/issues/138),
которые известны только по заголовкам и статусу.

## Два разбора, ради которых стоило копать

### `window.world` и читерство: что вообще можно сделать

**Саммари:** дыру можно сузить парой дешёвых приёмов, но закрыть её по-настоящему нельзя, не убрав
отладку из консоли или не унеся код игрока в песочницу. Пока игра одиночная, цена лечения выше цены
болезни; вопрос станет настоящим ровно в тот день, когда появится таблица лидеров.

Что именно происходит: `App.#startRun` (`src/pages/game/index.ts`) кладёт объект мира в
`window.world`, а код игрока
исполняется через косвенный `eval`, то есть в глобальной области. Объявления самой программы
прячет IIFE-обёртка, но цепочка областей видимости всё равно упирается в глобальную: из `init()`
виден весь граф симуляции — счётчики, лифты, пассажиры.

Варианты, от дешёвого к честному:

1. **Сделать счётчики геттерами поверх приватных полей.** `transportedCounter`, `avgWaitTime`,
   `maxWaitTime` перестанут присваиваться (в strict-коде — TypeError, в обычном — молча ничего).
   Правка маленькая, чтение из консоли сохраняется. Но это лежачий полицейский: `window.world.users`
   и `window.world.elevators` — настоящие объекты симуляции, а не фасады, и через них всё равно
   можно дотянуться до тех же счётчиков.
2. **Убрать `window.world` совсем.** Дыру закрывает: фасады `elevators`/`floors` держат свой лифт
   или этаж в приватном поле, доступа к миру из них нет. Цена — уходит отладка из консоли, которой
   пользуются решения из вики и половина баг-репортов; у нас это даже закреплено тестом «keeps the
   window.world debugging hook pointing at the live world» в `src/pages/game/index.test.ts`.
3. **Унести код игрока в Worker или iframe.** Единственный настоящий ответ: тогда недостижим не
   только мир, но и DOM (иначе можно просто дорисовать себе победу). Цена — своя: `console.log` в
   коде игрока и отладчик браузера работают уже не так прямо, а это то, чем в игре реально
   пользуются.

Вывод: пока результаты никуда не отправляются, вариант 1 даёт мало, вариант 3 стоит дорого. Держим
как есть и знаем цену; если появится зал славы — сначала пункт 3, потом всё остальное.

### `getExactFutureFloorIfStopped()` и `isApproachingFloor()`: не станет ли слишком легко

**Саммари:** сложность игры живёт в диспетчеризации, а не в кинематике, поэтому «слишком легко» не
станет — но уровни с ограничением по ожиданию просядут заметно, а публичный метод про точный этаж
остановки навсегда прибивает нашу внутреннюю физику к API. Разумный компромисс — дать точную позицию
и оставить предсказание игроку.

Подробно:

- **`isApproachingFloor(n)` сложности не убавляет — и он не «сахар» поверх `destinationDirection()`
  с `currentFloor()`, как кажется на первый взгляд.** Трижды не сахар. **Во-первых**, он смотрит на
  `velocityY`, а не на цель: лифт, которому только что
  выдали `goToFloor`, но который ещё не тронулся, отвечает `false` про все этажи, тогда как
  `destinationDirection()` уже говорит «вверх» — это разные вопросы, «куда собрался» и «куда едет».
  **Во-вторых**, он считает по точному дробному `y`, а `currentFloor()` округлён и переключается на
  середине пролёта: пока кабина едет со второго на третий, `currentFloor()` показывает 3 уже с
  половины пути, а `isApproachingFloor(3)` всё ещё `true`, потому что этаж действительно впереди.
  Этого полуэтажного зазора из публичного API не вывести никак — он есть в тестах
  `src/game/elevator-interface.test.ts`. **В-третьих**, и это главное, `Elevator.handleNewState`
  сторожит этим же предикатом каждое событие `passing_floor`, и фасад отдаёт его игроку без
  переписывания. Ценность метода не в экономии строчки, а в том, что у движка и у программы игрока
  одно понятие «этаж пройден» — самодельное сравнение направления с округлённым этажом даёт второе,
  расходящееся с событиями. Брать безопасно, и взято.
- **`getExactFutureFloorIfStopped()` даёт много.** Сегодня игрок не может посчитать тормозной путь:
  кривая разгона и торможения — внутренняя деталь движка, её нельзя вывести из публичного API, можно
  только измерить эмпирически. Метод превращает «подобрать по пути» из эвристики в точный расчёт, а
  именно на этом сыплются уровни с максимальным ожиданием. Это не убирает главную задачу — кому из
  лифтов ехать на какой вызов и в каком порядке, — но снимает целый класс промахов.
- **Главная цена не в сложности, а в контракте.** Публичный метод, возвращающий точный этаж
  остановки, фиксирует нашу физику: любая правка сглаживания движения после этого — ломающее
  изменение для чужих решений. У нас есть обязательство по поведенческой совместимости с оригиналом,
  и добровольно расширять его на детали кинематики не хочется.

Практический вывод: если брать, то `getExactCurrentFloor()` (точная позиция — безвредна и полезна
для диагностики) и, возможно, `isFull()`/`isEmpty()`. Предсказание этажа остановки — либо не брать,
либо давать явно как «продвинутый» метод с оговоркой, что он зависит от версии движка.

**Что из этого сделано:** `isFull()`, `isEmpty()` и `isApproachingFloor(n)` — то есть ровно те три,
про которые здесь написано «брать безопасно». `getExactFutureFloorIfStopped()` не взят, и по той
причине, что в третьем пункте: цена не в сложности, а в контракте. `getExactCurrentFloor()` не взят
пока — он безвреден, но и повода нет, пока никто не жаловался на диагностику.

**Отказ не просто записан, а закреплён тестом:** `src/game/elevator-interface.test.ts` держит
отдельный список того, чего на фасаде быть не должно, и оба имени стоят в нём. Код при этом занял
позицию жёстче документа — общий комментарий над обоими объясняет отказ кривой торможения, а это
довод ровно про первый метод: второй никакой кривой не считает, он просто отдаёт дробный `y`. Так
что пункт про `getExactCurrentFloor()` ниже живой, но браться за него значит сперва развести эти два
отказа, а не дописать метод. Та же оговорка, что и про `WorldOptions`: фасад с тех пор вырос —
`takeRequest`, `servedFloors` и `maxPassengerCount` пришли из главы 2, — так что «на фасад
ничего не добавляем» не правило, из которого этот отказ следует сам собой.

## Что просят снова и снова

По числу независимых упоминаний.

1. **Автокомплит и типы в редакторе** — [#133](https://github.com/magwo/elevatorsaga/issues/133),
   [PR #137](https://github.com/magwo/elevatorsaga/pull/137), три независимых `.d.ts`, плюс просьбы
   на [Lobsters](https://lobste.rs/s/w1dac5/) и [HN](https://news.ycombinator.com/item?id=37306262).
   Самый очевидный выигрыш.

   **Monaco**, который для этого берут в трёх форках, — редактор из VS Code, опубликованный
   отдельным пакетом; ценен он тем, что умеет запускать TypeScript-сервис в вебворкере, то есть по
   `.d.ts` даёт настоящие подсказки по типам и подчёркивание ошибок до запуска. Плата — вес: он в
   разы тяжелее нашего CodeMirror 6 (замеры у записи про jaredkrinke выше), не tree-shake-ится и
   заметно сложнее в сборке. Ценное здесь — декларации API, а не редактор.

   **Сделано, и именно этой половиной:** API лифта и этажа предлагается в автодополнении CodeMirror
   (`src/ui/completions.ts`) — источник добавлен к собственным источникам языка, а не вместо них.
   Декларации живут отдельным файлом для чужого редактора (`public/elevatorsaga.d.ts`); типов и
   подчёркивания ошибок до запуска в редакторе на странице по-прежнему нет.

2. **Нормальный вывод ошибок** — номера строк, подсветка, непроглоченные стеки. **Сделано:** ошибки
   компиляции и времени выполнения показываются баннером над редактором (`showError` в
   `src/widgets/editor-pane/ui/editor-pane.ts`), место сбоя подчёркивается волной с прокруткой к
   нему (`markError` в `src/ui/editor.ts`). В баннере не стек, а заголовок ошибки (`describeError`),
   плюс кнопка «перейти к строке N», которую строит `locateCodeError`. Дорогого шага, которого тут
   все ждут, — пересчёта позиций из стека в позиции редактора — не понадобилось:
   `getCodeObjFromCode` (`src/game/user-code.ts`) оборачивает исходник одной скобкой **без перевода
   строки**, так что номер строки в стеке и есть номер строки в редакторе, а единственная поправка —
   один столбец на первой строке, вынесенный в `firstLineColumnOffset`.
3. **Больше уровней, и свои собственные** —
   [#114](https://github.com/magwo/elevatorsaga/issues/114); magwo в 2015-м: «особенно я был бы рад
   помощи с добавлением уровней… ради хорошей кривой сложности». У 2xh это уже сделано наполовину
   (см. выше).
4. **Реалистичные профили трафика вместо равномерно случайных появлений** — joelthelion на HN, где
   **magwo подтвердил, что в коде есть неиспользованные распределения появления пассажиров**.
   Утренний поток вверх и вечерний вниз — заодно то, ради чего destination dispatch вообще
   интересен.

   **Сделано, вместе с пунктом 7 и раньше него.** `WorldOptions.trafficProfile` принимает `"mixed"`,
   `"up-peak"`, `"down-peak"` или `"lunch"` (`src/game/world.ts`); по умолчанию `"mixed"`, и это тот
   же поток, что рисовало каждое здание до появления опции, вплоть до отдельной раздачи. Пики
   сделаны чистыми, а не взвешенными: на `"up-peak"` вниз не едет никто. Реальный утренний поток —
   процентов 85 входящих, и недостающие 15 выброшены нарочно, иначе профиль пришлось бы замечать по
   смещению за много прогонов, а не видеть в первые десять секунд.

   Живёт это всё в **отдельной главе 2** — тринадцать уровней `chapter2-1`…`chapter2-13`
   (`src/game/chapter2.ts`), маршрут `#level=chapter2-N`. **В уровнях 1–19 и в восьми учебных нет ни
   одного `trafficProfile` и ни одного `destinationDispatch`**, и это условие, а не совпадение:
   вкрутить профили в оригинальные уровни значило бы кончить сравнимость результатов, которую мы
   держим намеренно.

5. **Рестарт по требованию, переключение уровней и управление скоростью времени** — в оригинале не
   было ничего из этого, кроме ссылки «Next challenge». **У нас сделано целиком, и все три вещи
   стоят в одном ряду в шапке страницы:** две кнопки прогона (`src/features/run-simulation/`),
   переключатель скорости (`src/features/adjust-speed/`, последняя ступень — прогон вообще без
   рисования) и переключатель уровней, раскрывающий поповер со всеми уровнями всех блоков. До
   двенадцатого не надо ни выигрывать одиннадцать, ни править адресную строку: каждая плитка —
   обычная ссылка, и заперта из них ни одна.
6. **Зал славы с ранжированием алгоритмов** —
   [zxcvbn4038](https://news.ycombinator.com/item?id=33249988). Обратите внимание на противоречие со
   строкой про `transportedCounter` выше.
7. **Destination dispatch — выбор этажа снаружи кабины** —
   [paxys](https://news.ycombinator.com/item?id=33249988). **Сделано 24 августа 2026 года.**
   `WorldOptions.destinationDispatch` — настройка всего здания, а не каждого этажа по отдельности:
   здание, где часть этажей с кнопками вызова, а часть без, построить можно, а играть в него плохо,
   потому что программе пришлось бы отвечать на два вида вызова сразу — это две механики,
   преподанные одновременно. Пассажир на таком этаже называет нужный этаж, а не направление: этаж
   поднимает `destination_requested` (`src/game/floor-interface.ts`), а программа бронирует под
   вызов кабину через `ElevatorInterface.takeRequest(fromFloorNum, toFloorNum)` — единственное
   добавление к фасаду, которое не просто выводит наружу то, что и так лежало на `Elevator`. Три
   уровня главы 2 построены на этом: `chapter2-11`, `chapter2-12` и `chapter2-13`. Порядок был важен и
   соблюдён — профили трафика приземлились раньше, и у каждого из этих трёх уровней есть свой
   профиль, так что диспетчеризация по назначению решает задачу, которая в игре к тому моменту уже
   была; без утреннего и вечернего потока решать ей нечего.
8. **Понятная диагностика провала** — [#135](https://github.com/magwo/elevatorsaga/issues/135);
   CobrastanJorji хотел видеть запланированные остановки каждого лифта и расчётное время прибытия.
   **Сделано наполовину.** У каждой кабины и у каждого этажа есть карточка, всплывающая по
   наведению и по фокусу (`src/widgets/building-stage/`): у кабины — состояние, занятость,
   индикаторы и **нажатые в ней кнопки**; у этажа — сколько человек ждёт, сколько ждёт самый давний
   и какие этажи они выбрали. Обеих вещей, которые просил именно CobrastanJorji, там нет:
   `destinationQueue` — план программы, а не нажатия пассажиров — не показывается нигде, и
   расчётного времени прибытия тоже нет. См. пункт 1 списка «Что делать дальше».
9. **`idle` должен срабатывать и при нажатии кнопки** —
   [pepijndevos](https://news.ycombinator.com/item?id=8929314). **Проверено: сообщение верное, но
   это решение, а не задача** — подробности в разделе «Что делать дальше», пункт про `idle`.
10. **Признак «кабина полна»** — [curmudgeon22](https://news.ycombinator.com/item?id=33249988);
    `isFull()` у 2xh — половина ответа. Просьба обоснованная: `loadFactor()` почти никогда не
    доходит до 1.0, потому что вес пассажира — случайные 55–100 кг (`createRandomUser` в
    `src/game/world.ts`) при норме 100 на место, так что полная кабина показывает в среднем ровно
    0.775 (avanderw наткнулся на то же самое и взял порог 0.7 на глаз, не выводя числа). **Сделано**
    и было самым дешёвым из всего списка: `isFull()` и `isEmpty()` уже лежали на `Elevator`, не
    хватало только фасада.
11. **Решения на любом языке, с оценкой ещё и по CPU и памяти** — wezm на Lobsters; форк с Go/WASM —
    единственный частичный ответ.
12. **Адаптивная вёрстка** — просили ещё в 2015-м те, кто преподавал с проектора. У нас сделано.

## Идеи из соседних игр

- **Гистограммы результатов вместо глобальной таблицы лидеров, как у Zachtronics.** Распределение
  среднего ожидания или числа ходов по уровню с отметкой «вы здесь» даёт соревнование, не создавая
  мишени для читерства — что важно с учётом строки про `transportedCounter`. Их обходной путь для
  соревнования среди своих — таблицы только среди друзей.
- **Экспорт зацикленной GIF-ки, как в Opus Magnum.** Артефакт, которым делятся, — это анимация
  работающего решения, а не ссылка.
- **[SIC-1](https://jaredkrinke.itch.io/sic-1/devlog)** — точки останова, слоты сохранений,
  статистика по задаче, встроенный справочник и по форумной ветке на задачу. Тот же автор, что у PR
  с Monaco.
- **Повтор комнаты в Screeps** — ползунок по тикам со ссылками на конкретный тик и то, что игроки
  попросили следом: **скроллбар, раскрашенный по событиям**, чтобы находить интересный тик, а не
  скроллить вслепую. У нас это была бы отметка там, где пассажир перешёл порог ожидания.
- **Повторы CodinGame** — файл повтора, который несёт всё, что вывел бот, плюс пошаговый ввод от
  рефери, и постоянная просьба «повторить в тех же условиях». **Сделано, кроме самого файла:** сид
  плюс фиксированный тик дают тот же прогон целиком (`src/game/random.ts`, разбор у форка avodonosov
  выше). Не хватает только артефакта: пересказать прогон другому человеку можно ссылкой на сид, а
  она требует, чтобы у того была та же программа.

## Что делать дальше

Ради этого списка документ и заводился. Порядок — оценочное суждение, а не факт; факты под ним
подписаны именами символов, по которым их можно проверить. Вычеркнутые отсюда пункты не отменены, а
сделаны, и разобраны выше — каждый в своём разделе.

1. **Запланированные остановки каждого лифта и расчётное время прибытия.** Половина сделана:
   карточки у кабин и этажей на сцене (`src/widgets/building-stage/`) закрывают панель состояния
   каждого робота из Cargo Dispatch. Осталось ровно то, что просил CobrastanJorji в
   [#135](https://github.com/magwo/elevatorsaga/issues/135) и чего в карточке нет: **план программы,
   а не нажатия пассажиров.** Данные лежат готовыми — очередь в `destinationQueue`, положение и
   скорость в `Elevator`, — и место, куда это дорисовать, теперь тоже есть, так что пункт
   подешевел. Расчётное время прибытия — второй вопрос: оно считается по кривой торможения, и
   показывать его на экране безопаснее, чем выдавать методом на фасаде (см. отказ по
   `getExactFutureFloorIfStopped` выше), но ту же арифметику написать придётся.
2. **Гистограммы результатов с отметкой «вы здесь».** Соревнование без глобальной таблицы, то есть
   без мишени для читерства через `window.world`. Считать распределение теперь есть чем: цикл по
   решениям через `npm run bench` с одним и тем же `--seeds` и есть та выборка, на фоне которой
   рисуется отметка, — раньше пункт стоял в очереди именно за этим.
3. **Заблокированные строки стартового кода** (приём из `gamesedu/elevatorsaga-gr`): выдать ученику
   скелет, который он не сломает. Сильно подешевело с тех пор, как в `src/ui/editor.ts` появился
   `EditorBuffer` со `starterCode` — место, куда это вставлять, уже есть. Подешевело и второй раз:
   у всех именованных уровней — восьми учебных и тринадцати из главы 2 — стартовая программа
   теперь своя у каждого (`startingCode` у уровня, `namedLevelBuffer` в редакторе), то есть тот
   самый скелет, который просит этот приём, уже раздаётся. Не хватает только запрета его править.
4. **`getExactCurrentFloor()` на фасаде.** Последним, и не потому что дорого: он безвреден и
   полезен для диагностики, но его никто ни разу не попросил. Прежде чем браться, прочитайте
   уточнение выше, в разборе про `getExactFutureFloorIfStopped`: имя стоит в списке запрещённых
   в `src/game/elevator-interface.test.ts`, под общим с ним комментарием, так что первый шаг —
   развести эти два отказа, а не дописать метод.

### Решения, которые не надо переоткрывать

Каждое из них уже обсуждено выше по документу. Их не надо решать заново — их надо знать.

- **`getExactFutureFloorIfStopped()` на фасаде — нет.** Причина не в сложности игры, а в контракте:
  публичный метод про точный этаж остановки навсегда прибивает нашу кинематику к API, и любое
  сглаживание движения после этого — ломающее изменение для чужих решений. Отказ записан не только
  здесь, но и в коде — в JSDoc у `ElevatorInterface.isApproachingFloor`.
- **Monaco — нет.** Ценны декларации, а не редактор; их и так можно скормить автодополнению
  CodeMirror, что и сделано. Разница в весе измерена выше.
- **Убирать `window.world` или уносить код игрока в песочницу — нет,** пока результаты никуда не
  отправляются. Геттеры поверх счётчиков дают мало, Worker или iframe стоит `console.log` и
  отладчика, а это то, чем в игре реально пользуются. Пересматривать в тот день, когда появится зал
  славы, и тогда сразу третьим вариантом.
- **Переменная скорость лифтов и разная высота этажей — нет.** Ломают сравнимость результатов с
  оригиналом. Соответственно `elevatorSpeeds`, `floorHeights` и `lobbyPossibility` не пущены и в
  `WorldOptions`, так что песочница до них не дотягивается. Проверено 25 августа 2026 года: все три
  по-прежнему отсутствуют. Но довод при них теперь надо предъявлять явно, а не выводить из привычки:
  `WorldOptions` за это время принял `elevatorServedFloors`, `trafficProfile` и
  `destinationDispatch`, так что «в опции мира ничего не добавляем» больше не правило, из которого
  этот отказ следует сам собой.
- **Неявный `checkDestinationQueue()` — нет.** Вариант реальный, и жалоба Ideka справедлива, но
  вызов — часть контракта, по которому написаны двенадцать лет чужих решений; ровно ту же причину мы
  предъявили minimusubi, отказавшись от их ломающего изменения в сигнатуре обработчиков. Метод
  задокументирован, поведение не тронуто.
- ~~**Экспорт GIF и скраббер повтора — нет.**~~ **Это решение отменено 25 августа 2026 года, и
  отменено не передумав, а потому, что довод под ним исчез.** Формулировка была: обе идеи требуют
  покадровой записи прогона, а прогон покадрово не воспроизводится в принципе — сид даёт здание и
  поток пассажиров, но не тайминг кадров, — так что это «не из чего», а не «дорого». **С переходом
  на фиксированный тик прогон воспроизводится потиково** (разбор у форка avodonosov выше,
  доказательство — `src/game/determinism.test.ts`): сид плюс та же программа дают ту же
  последовательность состояний на любом расписании кадров, а тик — это ровно та единица, по которой
  скраббер и ходил бы. Теперь это честное «дорого»: нужна запись состояния по тикам и второй режим
  рисования, читающий её вместо живого мира. Ни во что не упирается, но и в список «что делать
  дальше» само не просится — никто не просил, а два независимых запроса были на безголовый прогон,
  который уже есть.
- **Челлендж «вози полные кабины» — нет, во всяком случае не в одиночку.** Показатель
  `avgLoadFactorOnMove` (панель статистики, колонка в отчёте `npm run bench`) устроен так, что его
  максимум лежит не там, где хорошая игра. Замер на сидах 1–20, большой сценарий бенчмарка (18
  этажей, 6 кабин по 8, поток 1.9), три эталонные программы: объезд всех этажей по кругу (та, что
  напечатана в `docs/writing-solutions.md`) — 290.8 доставленных, среднее время доставки 46.1 с, загрузка **0.569**;
  обслуживание самого старого вызова первым — 326.8, 24.7 с, загрузка **0.485**; «стой на нулевом
  этаже, пока кабина не заполнится на 70%» — 192.5, 44.9 с, загрузка **0.721**. То есть самые полные
  кабины у программы, которая возит на треть меньше народу и держит людей в очереди вдвое дольше, а
  лучшая из трёх — с самой низкой загрузкой из трёх. Требовать высокой загрузки саму по себе —
  значит учить игрока копить пассажиров на этаже. Осмысленной эта величина становится только вторым
  условием рядом с доставкой: «довези столько-то за столько-то, не гоняя пустые кабины».

  **И ровно в этом виде она с тех пор и приземлилась**, так что запрет остаётся запретом только на
  ту половину, которой он и был: золото шестнадцатого уровня — `requireAll(underElapsedTime(70.1),
atLeastAvgLoadFactorOnMove(0.411))` (`src/game/levels.ts`), загрузка рядом с временем, а не
  вместо него. Порог 0.411 отсортирован прогонами, как и требовалось. Отдельно стоит знать, где
  тот же приём попробовали и отказались: у десятого уровня, где на выборке побед загрузка почти не
  коррелирует со временем прогона (около 0.03) — вторая ось там разделяла бы не игру программы, а
  везение раздачи. То есть решение «не в одиночку» подтвердилось дважды: один раз тем, что вторым
  условием работает, второй — тем, что даже вторым условием работает не везде.

### Решение, а не задача: `idle` при нажатии кнопки

Сообщение pepijndevos **верное**. Проверено по коду: `idle` поднимается ровно в одном месте — в
ветке пустой очереди у `ElevatorInterface.checkDestinationQueue`, — а движок зовёт этот метод из
`World.init`, из `goToFloor` и с путей прибытия и стоянки, но не на каждом кадре.
`World.#handleButtonRepressing` же пере-предлагает этаж только той кабине, у которой
`currentFloor === floor.level`, которая стоит, не полна и светит нужным индикатором, — то есть
только той, что уже припаркована на этом самом этаже. Значит, лифт, стоящий на нулевом этаже с
пустой очередью, действительно не проснётся от вызова с третьего.

Почему это всё равно не надо чинить — довод положительный, а не оборонительный. **Ответ игры на «мой
припаркованный лифт не реагирует на кнопку» — «подпишись на события кнопок», и с появлением
учебного трека этот ответ не оставлен на самостоятельное открытие, а прямо преподаётся.** Учебный
уровень 3 учит `floor_button_pressed` — кнопкам в кабине; его стартовая программа умеет только
`idle` и поэтому не довозит никого. Учебный уровень 5 учит кнопкам вызова на этажах
(`up_button_pressed` / `down_button_pressed`), и его TODO так и написан: «спроси у этажей, кому
нужен лифт, вместо того чтобы объезжать их все». Ответ пятого учебного уровня обходится вообще без
`idle`. То есть это не недоделка,
до которой у игры не дошли руки, а шов, по которому построен курс.

Первый прогон на этот шов не натыкается: стартовая программа в редакторе (`editor.defaultCode.code`)
на каждом `idle` заново ставит в очередь этажи 0 и 1, так что кабина никогда не остаётся стоять с
пустой очередью. Что и объясняет, почему на весь этот обзор жалоба ровно одна.

Ошибка, которой тут легко поддаться, — считать, что срабатывание `idle` по кнопке автоматически
сломало бы учебные уровни. Не сломало бы: обработчик `idle` в стартовой программе третьего уровня
умеет только `goToFloor(0)`, так что сколько его ни будить, пассажиров он не развезёт. Довод в
другом — в том, что события кнопок перестали бы быть единственным ответом на вопрос «как узнать о
вызове», а именно этому вопросу посвящены два из восьми уровней трека. Поэтому: не планировать, а
решать отдельно.

## Тупики — сюда больше не ходить

- **«The Elevator Saga» на thecasecentre.org** — бизнес-кейс про оплату лифта в будапештском доме.
  Совпадение названий.
- **[onlyred/elevator_saga](https://github.com/onlyred/elevator_saga)** — трёхфайловая игрушка на
  Python, заброшена.
- **Китайские источники**: только зеркала, репосты на CSDN/OSCHINA и разборы решений. Ни расширенной
  версии, ни перевода не нашлось, при поиске по 汉化 / 增加关卡 / 重写. Один пост на CSDN отказал в
  соединении.
- **Reddit** из этого окружения не читается: r/programming и r/javascript считать **непокрытыми**, а
  не пустыми. [Тред на MetaFilter](https://www.metafilter.com/146807/) отдаёт 403.
- **Порта на Rust** не найдено.
- **`MR-Stan`, `Zarel`, `jeffersonhwang`, `robertleeplummerjr`, `runofthemillgeek`, `nivoset`,
  `koalazak`, `rasata`, `lowenhere`, `dominikgoss`, `Endika`** — сравнены, собственных коммитов нет,
  отстают или вровень с оригиналом. Их большой размер репозитория — это вендоренные `libs/`, а не
  новая работа.
