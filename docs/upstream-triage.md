# Upstream's open tracker, triaged against this fork

Compiled **12 August 2026**. Every verdict below was decided by reading this repository, not by
reading upstream's code or the threads' conclusions.

Upstream state at that date: **41 open items — 29 issues and 12 pull requests**, read through the
GitHub API on 12 August 2026. `master` at [magwo/elevatorsaga](https://github.com/magwo/elevatorsaga)
ends at `e0c55bf8`, a README edit dated 4 January 2021; the last commit that changed any code is
`9a2441af`, 21 July 2015. The tag `legacy-1.x` in this repository points at `e0c55bf8`, so anything
claimed here about "what upstream does" can be checked with `git show legacy-1.x:<file>`.

Two numbers are worth correcting while they are in front of us. `docs/fork-survey.md` opens by
saying there have been no commits since 21 November 2022; that date is the repository's last _push_,
which was the branch behind PR #137. `master` itself has not moved since January 2021 and has had no
code change since July 2015. And the tracker is smaller than it looks from the outside: 41 open
items against 91 closed ones.

Three verdicts are used, and they mean different things:

- **Closed here** — the complaint cannot be reproduced against this repository, and there is a named
  piece of code that is the reason why. A plausible-looking fix was not enough to earn this verdict;
  where the evidence is a measurement rather than a reading, the measurement is described.
- **Open here** — the defect or the gap is present now. Each entry says what would have to change
  and where.
- **Not applicable** — the code the report names is gone, the report is not a request, the behaviour
  is deliberate, or the patch is against a document this fork no longer has.

References to this repository's code are by symbol and file rather than by line, for the reason
`docs/fork-survey.md` gives: line numbers rot on the next commit and a symbol name is always
greppable. References to `legacy-1.x` do carry line numbers, because a tag does not move.

## Closed here

Sixteen issues and six pull requests. Eighteen of the twenty-two are claimed in README, under
"Fixed bugs" or "Asked for upstream, and here already". Four are recorded only here: #30, #57,
#120 and PR #115. The four most recent additions — #52 and PR #82, then #108 and PR #113 — each
stood on the shortlist below until the work landed, all of it on 12 August 2026; the rows are the
record of what closed them.

| Item                                                      | What it reports                                                             | Why it is closed here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#3](https://github.com/magwo/elevatorsaga/issues/3)      | The floor object handed to player code exposes the whole internal floor     | `FloorInterface` in `src/game/floor-interface.ts` is a facade with `floorNum()`, `level`, a `buttonStates` snapshot rebuilt on every read, and three events. Added in `ab039a3`; the file's docblock names the issue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [#30](https://github.com/magwo/elevatorsaga/issues/30)    | Wanted: a challenge type combining a move limit with a wait limit           | `requireUserCountWithinMovesWithMaxWaitTime` in `src/game/challenges.ts`, used by challenge 19 (100 people, 450 moves, 30 s). Commit `2e90995`, which sized the limits against six reference programs over twenty seeds and moved the perpetual demo to slot 20.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| [#34](https://github.com/magwo/elevatorsaga/issues/34)    | Wanted: replay a challenge with the same passengers                         | `#seed=…`. `createRandomSource` in `src/game/random.ts` (`3cf9d08`), `World.seed`, `resolveSeed` in `src/app/router.ts`; `src/game/determinism.test.ts` holds three seeds across frame rates that differ, wander, and differ by a nanosecond. The reporter also asked that a replay not count as a pass — it does count here, and README says why.                                                                                                                                                                                                                                                                                                                                                                              |
| [#52](https://github.com/magwo/elevatorsaga/issues/52)    | Wanted: an average time-spent-in-the-elevator statistic                     | `World.avgPickupTime` and `World.maxPickupTime`, taken at the `entered_elevator` event whose sibling `exited_elevator` the world already listened for (`8785041`), reported by the headless benchmark (`af53e6d`), and drawn on the panel between the two delivery clocks as `Avg wait for a car` (`ea9b51c`). The ride the issue asks for is the difference between two rows that now sit side by side; no row states that subtraction. `avgWaitTime` and `maxWaitTime` keep both their names and their meaning, deliberately: the challenge conditions read `maxWaitTime` through `ChallengeWorldStats` and every posted score is written against it, so what shipped is a figure beside them and not a redefinition of them. |
| [#57](https://github.com/magwo/elevatorsaga/issues/57)    | Wanted: a way to see whether a floor's call button is lit                   | `buttonStates` on `FloorInterface`, plus the `buttonstate_change` event; both are typed in `public/elevatorsaga.d.ts`. The property is deliberately left out of the table in `documentation.html` in favour of the event, the comment there explains why, and `src/page.test.ts` pins the omission so it cannot drift back in silently.                                                                                                                                                                                                                                                                                                                                                                                         |
| [#59](https://github.com/magwo/elevatorsaga/issues/59)    | People do not reconsider entering a stopped elevator when its lights change | The `indicatorstate_change` handler in `src/game/elevator.ts` re-offers boarding when the car is standing at a floor and is not full. Commit `38e7390`, refined by `84b7541` and `f21436c`. Verified by reproduction, not by reading: with indicators dark, none of 31 spawned passengers board a parked car; with the player lighting one on a hall call, they do.                                                                                                                                                                                                                                                                                                                                                             |
| [#68](https://github.com/magwo/elevatorsaga/issues/68)    | <kbd>Ctrl</kbd>/<kbd>Cmd</kbd>+<kbd>S</kbd> should save the code            | The editor claims the binding at `Prec.highest` in `src/ui/editor.ts` (`15ddba5`), suppresses the browser default, and writes immediately rather than waiting out the autosave delay. Covered end to end.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| [#92](https://github.com/magwo/elevatorsaga/issues/92)    | Stopping between floors never produces an `idle` event                      | `ElevatorInterface` re-checks the destination queue whenever the car halts, not only when it halts at the head of the queue. Commit `d885417`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [#98](https://github.com/magwo/elevatorsaga/issues/98)    | Passengers do not enter waiting elevators                                   | The same code as #59, and the same reproduction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| [#103](https://github.com/magwo/elevatorsaga/issues/103)  | Wanted: a playground with no success condition, for debugging               | `#challenge=sandbox`. `requireSandbox` and `createSandboxChallenge` in `src/game/challenges.ts`, `SANDBOX_LIMITS` and `resolveSandboxOptions` in `src/app/router.ts` (`db2f7d6`); four parameters, each clamped, and `fitElevatorCount` trims a car count that would not fit on screen.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| [#105](https://github.com/magwo/elevatorsaga/issues/105)  | The elevator moves off while passengers are still entering                  | The one-second dwell (`WAIT_AT_FLOOR_SECONDS` in `src/game/elevator-interface.ts`) now covers every path that halts a car, including the re-offer. Commits `d885417` and `5e56911`. Nothing to do with the statistics panel being clipped in a short building, which `d607ca4` fixed and calls "#105" in its first line: that number is a local task number and the panel was never reported upstream at all. The message is wrong there and stays wrong; this row is the correction.                                                                                                                                                                                                                                           |
| [#108](https://github.com/magwo/elevatorsaga/issues/108)  | How is a player supposed to know how many elevators there are?              | The prose says it now, where the question was asked: `docs.basics.called.html` in both catalogues, and the paragraph under "Basics" in both pages, state that `init` and `update` are handed the same two arrays and that `elevators.length` is how many cars there are (`309dabe`). The declarations in `public/elevatorsaga.d.ts` and the completion popup had the fact before that; what the issue reported was that the page a beginner reads did not.                                                                                                                                                                                                                                                                      |
| [#110](https://github.com/magwo/elevatorsaga/issues/110)  | A passenger refused for direction never presses the call button again       | `User.elevatorAvailable` presses it again when it declines a car heading the wrong way. Commit `aef3862`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| [#119](https://github.com/magwo/elevatorsaga/issues/119)  | Turn off the auto-formatter, which mangles pasted code                      | CodeMirror 6 without `indentOnInput` on the paste path (`15ddba5`), pinned by a genuine paste in `8602a42` and again in the end-to-end suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [#120](https://github.com/magwo/elevatorsaga/issues/120)  | Sharing the site produces no preview image                                  | `og:image`, `og:image:alt` and `twitter:card` in `index.html` (`83559e7`). The URL is root-relative on purpose: the comment above it records that upstream's absolute URL 404s today and that crawlers resolve relative ones against the page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [#133](https://github.com/magwo/elevatorsaga/issues/133)  | Wanted: TypeScript declarations for the player API                          | `public/elevatorsaga.d.ts` (`5c63aaf`), checked against the live facades by `src/api-declarations.test.ts` rather than maintained by hand and hoped over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| [PR #74](https://github.com/magwo/elevatorsaga/pull/74)   | Let users re-enter an elevator that changes indicators without moving       | The defect is gone, by a different route — see #59. What shipped re-offers from the elevator's own indicator change; the PR re-offers from the world's button-press pass, which here is still the separate legacy path in `World`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [PR #82](https://github.com/magwo/elevatorsaga/pull/82)   | Split the reported time into waiting, travelling and total                  | The same work as #52, and it shipped there: `User.pickupTimestamp` is set from the `entered_elevator` event, beside the `spawnTimestamp` that was already there. Two of the three figures it asks for are on the panel — the wait and the total — and the third, the time spent travelling, is the difference between them.                                                                                                                                                                                                                                                                                                                                                                                                     |
| [PR #113](https://github.com/magwo/elevatorsaga/pull/113) | `init` and `update` are described in one sentence that says almost nothing  | Both of the PR's facts are in that paragraph now (`309dabe`), with two the PR does not have: `init` runs on the first frame of the run rather than when the code is applied, and an arrow function does not get the object as its `this` -- player code is compiled by an indirect `eval` and runs sloppy in global scope, so an arrow `init` is handed `globalThis`. The PR's `dt` comment in the starter code was already here.                                                                                                                                                                                                                                                                                               |
| [PR #115](https://github.com/magwo/elevatorsaga/pull/115) | The floor argument to `up_button_pressed` is undocumented                   | The handlers are passed the facade, `documentation.html` documents the parameter, and `public/elevatorsaga.d.ts` types it. The facade is what is passed, which is stronger than what the PR asked for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [PR #117](https://github.com/magwo/elevatorsaga/pull/117) | `moveCount` starts at 1 because initial placement counts as a move          | `World` sets the floor position before the first `moveTo`. Commit `40d31a2`, which closes #20 as well.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [PR #137](https://github.com/magwo/elevatorsaga/pull/137) | Wanted: autocompletion inside the game                                      | `playerApiCompletionSource` in `src/ui/completions.ts` (`5f6932a`), over CodeMirror 6. The PR's Monaco is declined deliberately, and `docs/fork-survey.md` records the reasoning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Open here

Seven issues and five pull requests. Five of the twelve are partial — something addressing the
report shipped and the rest of it did not — and each of those says which half is which.

| Item                                                      | What it reports                                                      | Where it stands here, and what would have to change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [#4](https://github.com/magwo/elevatorsaga/issues/4)      | Too many challenges fall to a program that barely tries              | Present, and measured rather than assumed. Challenges 1–18 carry upstream's parameters unchanged and `src/game/challenges.test.ts` pins them on purpose. roryokane's 2015 naive program — drive to the nearest pressed floor, ignore direction — was run here over 100 seeds per challenge on a headless harness built from `createFrameRequester` and `WorldController`: challenge 1 passes on every seed, challenge 3 on 97, challenge 2 on 41, challenge 4 on none. Closing this means new or retuned challenges.                                                                                                                                                                             |
| [#33](https://github.com/magwo/elevatorsaga/issues/33)    | Wanted: a single `hall_button_pressed` event on the floor            | Absent. `FloorInterfaceEvents` in `src/game/floor-interface.ts` carries `buttonstate_change`, `up_button_pressed` and `down_button_pressed`, and nothing else. The workaround the thread settles on still works here: one registration naming both events space-separated is passed the event name as an extra first argument (`src/game/observable.ts`). Adding the event is one entry in the type and one forwarder in the constructor, then the whole documented surface.                                                                                                                                                                                                                     |
| [#46](https://github.com/magwo/elevatorsaga/issues/46)    | Documentation should be generated rather than hand-written           | Nothing is generated. One API fact is written by hand in `documentation.html`, `documentation.ru.html`, the `docs.*` keys of `src/i18n/en.ts` and `src/i18n/ru.ts`, `public/elevatorsaga.d.ts` and `src/ui/completions.ts`. What the issue actually fears — the copies drifting apart — is closed by tests instead: `src/page.test.ts` compares the two pages as one document, `src/api-declarations.test.ts` checks the declarations against the facades. The duplication is real; the rot is not.                                                                                                                                                                                              |
| [#66](https://github.com/magwo/elevatorsaga/issues/66)    | Wanted: challenge details readable from player code                  | Absent. `UserCodeObject` in `src/game/world-controller.ts` is handed `elevators` and `floors` and nothing else. Capacity is readable (`maxPassengerCount()`) and floor count is `floors.length`, but the challenge, its condition and the spawn rate are not: `World`'s `#spawnRate` and `#floorCount` are private, and even `window.world`, which this fork keeps deliberately, does not expose the spawn rate. Reading `location.hash` is the workaround the issue complains about.                                                                                                                                                                                                            |
| [#77](https://github.com/magwo/elevatorsaga/issues/77)    | Wanted: show how long a passenger has been waiting                   | Half, and a different half than before. The wait is now measured and reported: `World.avgPickupTime` and `World.maxPickupTime`, on the panel as `Avg wait for a car` (see #52). What the issue asks for is that figure _beside the passenger_, and no number is drawn over anybody's head. The yellow highlight on the longest-waiting passenger — `User.waitingLongest`, `presentUser`, and `.user.waiting-longest` in `src/styles/style.css` — also still ranks by the commute rather than by the wait: `World.update` compares `elapsedTime - spawnTimestamp` over everyone not yet delivered, so the passenger it marks may be one riding in a car rather than one left standing on a floor. |
| [#107](https://github.com/magwo/elevatorsaga/issues/107)  | Wanted: a way to import libraries into a solution                    | No support, though it is possible. Player code is evaluated by an indirect `eval` in global scope (`src/game/user-code.ts`) and there is no CSP, so dynamic `import()` and appending a `<script>` both work today; what is missing is anywhere to await the result, since `init` and `update` are called synchronously per frame. This fork moved away from the issue in one respect: `_`, `$` and `riot` used to be on `window` and are gone.                                                                                                                                                                                                                                                   |
| [#114](https://github.com/magwo/elevatorsaga/issues/114)  | Wanted: levels modelled on real buildings — weight, nuisance calls   | Absent as behaviour. Weight exists (`createRandomUser` draws 55–100, and `Elevator.getLoadFactor` is its only consumer) but changes nothing about speed, dwell or capacity. There are no nuisance calls, no wrong-direction presses, no override mode: `User.pressFloorButton` always presses the correct direction. Already on this fork's own backlog in `docs/fork-survey.md`.                                                                                                                                                                                                                                                                                                                |
| [PR #73](https://github.com/magwo/elevatorsaga/pull/73)   | Floor numbers are hard to read; the waiting area wastes space        | Half. Readability is fixed by other means: the floor number is `rgb(255 255 255 / 60%)` here against `rgba(255, 255, 255, 0.15)` at `legacy-1.x:style.css:336`. The layout half is untouched — the PR folds the number and the call indicator into one `.floor-info` box and moves the elevator bank from x=200 to x=100, and `FIRST_ELEVATOR_X` in `src/game/world.ts` is still 200 with upstream's spawn band beside it.                                                                                                                                                                                                                                                                       |
| [PR #87](https://github.com/magwo/elevatorsaga/pull/87)   | Wanted: scripted traffic patterns instead of uniform random arrivals | Absent. `spawnUserRandomly` is the only spawn path, `WorldOptions` has no `spawnPattern`, and arrivals come from one uniform accumulator with a hard-coded even split between the lobby and everywhere else. The PR's shape — a list of `{time, fromFloor, toFloor, numPeople, spawnProbability}` entries and a challenge with `spawnRate: 0` — is a usable starting point.                                                                                                                                                                                                                                                                                                                      |
| [PR #93](https://github.com/magwo/elevatorsaga/pull/93)   | Two new challenge conditions built on the move counter               | Half. `requireUserCountWithinMovesWithMaxWaitTime` shipped, under a name matching the existing `…WithinTimeWithMaxWaitTime` (see #30). The PR's other condition, `requireUserCountWithinMovesAndTime` — a move limit and a wall-clock limit together — has no equivalent here.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [PR #104](https://github.com/magwo/elevatorsaga/pull/104) | Wanted: a control to expand and collapse the code editor             | Absent. The editor's height is the fixed `--editor-height` custom property in `src/styles/style.css`, overridden once in a narrow-viewport media query, and nothing on the page changes it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [PR #132](https://github.com/magwo/elevatorsaga/pull/132) | "Only the very best programs" should read "programmers"              | The sentence is unchanged here, in `documentation.html`, `documentation.ru.html` and the `docs.about.p2.html` key of both catalogues. Whether it is a defect is a judgement rather than a fact: this game scores programs and not people, and upstream's wording reads deliberate.                                                                                                                                                                                                                                                                                                                                                                                                               |

## Not applicable

Six issues and one pull request.

| Item                                                      | What it reports                                          | Why it does not apply                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#94](https://github.com/magwo/elevatorsaga/issues/94)    | The test page errors with "Object has no method 'trunc'" | The page is gone: no `test/` directory, no Jasmine, and the runners are Vitest and Playwright. `Math.trunc` survives in exactly one place, `Elevator`'s passing-floor check, and the build targets ES2022 — a browser without `Math.trunc` cannot load the ES module at all.                                                            |
| [#96](https://github.com/magwo/elevatorsaga/issues/96)    | "This was amazingly fun!"                                | A thank-you note. There is no request in it.                                                                                                                                                                                                                                                                                            |
| [#101](https://github.com/magwo/elevatorsaga/issues/101)  | On MS Edge, "Next Challenge" does nothing                | Filed against EdgeHTML, which no longer exists; today's Edge is Chromium. The control here is a plain anchor served by the router's hash handling, so the reported code path is gone as well as the browser.                                                                                                                            |
| [#106](https://github.com/magwo/elevatorsaga/issues/106)  | Code cannot be selected on Android Chrome                | Filed against CodeMirror 5, which this fork replaced wholesale with CodeMirror 6 (`15ddba5`). Anything remaining would be a new report against different software, and nothing in the old one transfers.                                                                                                                                |
| [#111](https://github.com/magwo/elevatorsaga/issues/111)  | Only the last configured elevator responds to handlers   | Not a bug, and diagnosed rather than dismissed: a `var` loop gives every handler one shared binding. README explains it, `documentation.html` explains it to players, and `src/game/world-controller.test.ts` asserts the engine half so that nobody ever repairs the simulation for a fault that is not in it.                         |
| [#124](https://github.com/magwo/elevatorsaga/issues/124)  | A user does not enter an elevator stopped en route       | Not a bug. `stop()` called from a `passing_floor` handler leaves the car between floors, and boarding is offered on arrival at a floor; `goToFloor` is what the reporter wanted. `src/game/world.test.ts` reproduces it both ways under "stopping en route". This was listed here as fixed until 12 August 2026, and never was.         |
| [PR #116](https://github.com/magwo/elevatorsaga/pull/116) | A Japanese translation of the documentation              | Not applicable as a patch: it translates upstream's `documentation.html`, and this fork's documentation is a different document living in two HTML files and 83 `docs.*` catalogue keys. The machinery for a third locale exists (`LOCALES`, `CATALOGUE_LOADERS`, `documentationUrl`); the PR's contribution is text that is now wrong. |

## What is worth doing

Ranked by what it buys this repository, not by how loud the thread was.

The list opened with splitting the commute into waiting and travelling, which was the largest of
them and is now done: `8785041`, `af53e6d` and `ea9b51c` closed #52 and PR #82 and took #77 down to
its remaining half, and the estimate it was written against — a timestamp on `User`, two figures on
`World`, two rows and their catalogue keys — turned out to be the right shape. It is struck from
here rather than restated; the "Closed here" rows say what shipped, and #77 says what did not.

1. **Say what `init` and `update` are actually handed** — closes PR #113 and #108. Two facts, both
   true today and neither written down: `this` is the object the player declared, and `update` runs
   every frame. _Cost:_ one paragraph in `documentation.html`, its counterpart in
   `documentation.ru.html`, and the `docs.basics.called.html` key in both catalogues. _Risk:_
   effectively none; `src/page.test.ts` fails if one language is left behind.

2. **Decide about #4 in public** — the complaint is real and now measured, and the fork has never
   answered it. _Cost:_ very high if the answer is new challenges, since each needs sizing against
   reference programs the way `2e90995` did; near zero if the answer is to leave the ladder alone
   and say so in README. _Risk:_ the expensive option is also the dangerous one — retuning
   challenges 1–18 breaks this fork's own promise that a legacy solution is scored by the same
   rules, which README states and `src/game/challenges.test.ts` enforces. A nineteenth challenge
   was the safe shape of this answer, and more of the same is the safe continuation.

3. **Let the editor grow** — PR #104, the only open item that is purely about using the game.
   _Cost:_ a button, a persisted flag, and turning `--editor-height` into something the page can
   set. _Risk:_ low; the two places to remember are the narrow-viewport media query that also sets
   the property, and telling CodeMirror to remeasure after the change.

4. **Add `hall_button_pressed`** — #33, ten years old and never given an answer beyond a
   workaround. _Cost:_ two lines in `src/game/floor-interface.ts` and then the entire documented
   surface — both HTML pages, both catalogues, the declarations and the completions, each with the
   tests that pin them. _Risk:_ low technically, but it widens the gap between this fork's player
   API and upstream's, which README currently describes as three methods.

5. **Scripted traffic** — PR #87, and item 2 of this fork's own backlog in `docs/fork-survey.md`.
   _Cost:_ a `spawnPattern` option on `WorldOptions`, a schedule pass in `World.update`, and a
   decision about which random stream it draws from. _Risk:_ moderate, and concentrated in one
   place: the draw order in `spawnUserRandomly` is load-bearing for seed replay, so a new spawn
   path has to take its randomness from a derived stream or every existing seed changes meaning.

6. **Horizontal room in the waiting area** — the unshipped half of PR #73. _Cost:_ moving
   `FIRST_ELEVATOR_X` and the spawn band, which invalidates every screenshot and any end-to-end
   assumption about coordinates. _Risk:_ moderate cost for a cosmetic gain, and the pressure it
   relieves is partly relieved already by `fitElevatorCount`. Worth doing only alongside other
   layout work.

7. **PR #132's one word** — _Cost:_ four strings. _Risk:_ none, but it should be adopted only if
   the maintainer agrees with the reading, which is arguable; "programs" is not obviously a typo.

Left off deliberately: **#46** (the duplication is real, but generation is a large refactor and the
drift it would prevent is already prevented by tests), **#66** and **#107** (both would widen the
player API's contract for a small audience, and #107 already works in practice), **#114** (a design
project rather than a fix), and **PR #93's** second condition (a move limit plus a wall-clock limit
is a fourth way to say what three existing conditions already say).
